# Prometheus scrape configs — VertexChain custom exporters
# Append these scrape_configs entries to prometheus.yml

- job_name: "vertexchain-gist-exporter"
  static_configs:
    - targets: ["vertexchain-monitoring:9101"]
  metrics_path: /metrics
  scrape_interval: 30s
  scrape_timeout: 10s
  relabel_configs:
    - source_labels: [__address__]
      target_label: instance
      replacement: "gist-exporter"

- job_name: "vertexchain-soroban-exporter"
  static_configs:
    - targets: ["vertexchain-monitoring:9102"]
  metrics_path: /metrics
  scrape_interval: 30s
  scrape_timeout: 15s
  relabel_configs:
    - source_labels: [__address__]
      target_label: instance
      replacement: "soroban-exporter"

- job_name: "vertexchain-postgres-exporter"
  static_configs:
    - targets: ["postgres-exporter:9187"]
  metrics_path: /metrics
  scrape_interval: 30s
  scrape_timeout: 10s
